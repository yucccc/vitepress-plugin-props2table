import { defineConfig } from 'vitepress'

export default defineConfig({
  title: 'props2table',
  base: '/vitepress-plugin-props2table/',
  themeConfig: {
    socialLinks: [
      { icon: 'github', link: 'https://github.com/yucccc/vitepress-plugin-props2table' }
    ],
  }
})